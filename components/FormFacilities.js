import React, { Component } from 'react';
import { View, Text } from 'react-native';

class FormFacilities extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> FormFacilities </Text>
      </View>
    );
  }
}

export default FormFacilities;
